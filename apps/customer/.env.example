# RescueBite — Customer mobile app (Expo)
# Copy to .env. Expo exposes EXPO_PUBLIC_* vars to the JS bundle.

# Base URL of the RescueBite API.
# Use your machine's LAN IP (not localhost) when testing on a physical device.
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000
